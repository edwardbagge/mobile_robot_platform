# test_13 config

This directory is reserved for future lidar test configuration such as:

1. RViz profiles.
2. Static transform defaults for `laser` to `base_link`.
3. Driver-specific parameter snapshots.
