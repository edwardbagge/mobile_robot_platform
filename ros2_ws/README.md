# ROS 2 Workspace Notes

This folder is my clean ROS 2 workspace.

Current goal:
- Start from zero
- Learn the setup steps properly
- Keep personal notes on what I did and why

Basic workspace layout:
- `src/` contains ROS 2 packages
- `build/` is generated by `colcon build`
- `install/` is generated by `colcon build`
- `log/` is generated by `colcon build`

My setup notes are in [notes.md](./notes.md).
